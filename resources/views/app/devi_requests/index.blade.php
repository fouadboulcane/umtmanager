@extends('layouts.app')

@section('content')
<div class="container">
    <div class="card">
        <div class="card-body">
            <div style="display: flex; justify-content: space-between;">
                <h4 class="card-title">
                    @lang('crud.devi_requests.index_title')
                </h4>
            </div>

            <div class="searchbar mt-4 mb-5">
                <div class="row">
                    <div class="col-md-6">
                        <form>
                            <div class="input-group">
                                <input
                                    id="indexSearch"
                                    type="text"
                                    name="search"
                                    placeholder="{{ __('crud.common.search') }}"
                                    value="{{ $search ?? '' }}"
                                    class="form-control"
                                    autocomplete="off"
                                />
                                <div class="input-group-append">
                                    <button
                                        type="submit"
                                        class="btn btn-primary"
                                    >
                                        <i class="icon ion-md-search"></i>
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                    <div class="col-md-6 text-right">
                        @can('create', App\Models\DeviRequest::class)
                        <a
                            href="{{ route('devi-requests.create') }}"
                            class="btn btn-primary"
                        >
                            <i class="icon ion-md-add"></i>
                            @lang('crud.common.create')
                        </a>
                        @endcan
                    </div>
                </div>
            </div>

            <div class="table-responsive">
                <table class="table table-borderless table-hover">
                    <thead>
                        <tr>
                            <th class="text-left">
                                @lang('crud.devi_requests.inputs.content')
                            </th>
                            <th class="text-left">
                                @lang('crud.devi_requests.inputs.manifest_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.devi_requests.inputs.client_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.devi_requests.inputs.user_id')
                            </th>
                            <th class="text-left">
                                @lang('crud.devi_requests.inputs.status')
                            </th>
                            <th class="text-center">
                                @lang('crud.common.actions')
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @forelse($deviRequests as $deviRequest)
                        <tr>
                            <td>
                                <pre>
{{ json_encode($deviRequest->content) ?? '-' }}</pre
                                >
                            </td>
                            <td>
                                {{ optional($deviRequest->manifest)->title ??
                                '-' }}
                            </td>
                            <td>
                                {{ optional($deviRequest->client)->name ?? '-'
                                }}
                            </td>
                            <td>
                                {{ optional($deviRequest->assignedMember)->name
                                ?? '-' }}
                            </td>
                            <td>{{ $deviRequest->status ?? '-' }}</td>
                            <td class="text-center" style="width: 134px;">
                                <div
                                    role="group"
                                    aria-label="Row Actions"
                                    class="btn-group"
                                >
                                    @can('update', $deviRequest)
                                    <a
                                        href="{{ route('devi-requests.edit', $deviRequest) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-create"></i>
                                        </button>
                                    </a>
                                    @endcan @can('view', $deviRequest)
                                    <a
                                        href="{{ route('devi-requests.show', $deviRequest) }}"
                                    >
                                        <button
                                            type="button"
                                            class="btn btn-light"
                                        >
                                            <i class="icon ion-md-eye"></i>
                                        </button>
                                    </a>
                                    @endcan @can('delete', $deviRequest)
                                    <form
                                        action="{{ route('devi-requests.destroy', $deviRequest) }}"
                                        method="POST"
                                        onsubmit="return confirm('{{ __('crud.common.are_you_sure') }}')"
                                    >
                                        @csrf @method('DELETE')
                                        <button
                                            type="submit"
                                            class="btn btn-light text-danger"
                                        >
                                            <i class="icon ion-md-trash"></i>
                                        </button>
                                    </form>
                                    @endcan
                                </div>
                            </td>
                        </tr>
                        @empty
                        <tr>
                            <td colspan="6">
                                @lang('crud.common.no_items_found')
                            </td>
                        </tr>
                        @endforelse
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="6">{!! $deviRequests->render() !!}</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>
@endsection
